/**
 * Fixed scroll margin below the sticky header (matches globals.css --scroll-offset).
 * Use this class on section anchors — avoids CSS var in classNames for stable SSR hydration.
 */
export const SECTION_SCROLL_MT = "scroll-mt-[4.75rem] sm:scroll-mt-[5.25rem]";
